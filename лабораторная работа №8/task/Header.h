#pragma once       
#include <stdio.h>             
#include <stdlib.h>			   
#include <Windows.h>           

void sortStrings(char**, int, int);
int str_len(char*);
char** inputStrings(int, char**);
void printStrings(char**, char*, int);
